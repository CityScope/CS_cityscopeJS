import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles({
    root: {
        width: "80%",
        marginLeft: 28,
    },
    subtitle1: {
        marginTop: 12,
    },
    subtitle2: {
        marginTop: 12,
    },
});
